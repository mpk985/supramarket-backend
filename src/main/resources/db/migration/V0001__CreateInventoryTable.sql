create table car_inventory (
inventory_id bigserial primary key,
year varchar,
make varchar,
model varchar,
trim varchar,
transmission varchar,
color varchar,
price varchar,
vin varchar,
car_sold varchar,
product_img varchar
)